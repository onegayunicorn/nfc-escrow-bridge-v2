# NFC Escrow Bridge v2.0 - Requirements
# ================================
# Stripe Integration Dependencies

# Core Stripe SDK
stripe>=7.0.0

# Data validation and models
pydantic>=2.0.0

# Async support
asyncio-mqtt>=0.12.0

# Web framework (for API server)
fastapi>=0.100.0
uvicorn>=0.20.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# HTTP client
httpx>=0.25.0
aiohttp>=3.8.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Machine learning (for risk engine)
scikit-learn>=1.3.0

# Redis (for caching/sessions)
redis>=5.0.0

# Environment variables
environ>=1.0.0
python-dotenv>=1.0.0

# Logging
structlog>=23.0.0

# Type hints (Python 3.9+)
typing-extensions>=4.0.0

# Development dependencies
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
isort>=5.12.0

# Optional: For production deployment
gunicorn>=21.0.0
prometheus-client>=0.17.0
opentelemetry-api>=1.18.0
opentelemetry-sdk>=1.18.0
